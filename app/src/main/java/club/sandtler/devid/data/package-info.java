/**
 * Contains anything related to data management (caching)
 * and retrieval (network operations).
 * <p>
 * When requesting data, one should always use the corresponding
 * {@code Repository} classes to do so.  They maintain a local cache and will
 * therefore offer the best performance.
 */

package club.sandtler.devid.data;
